From 0ddbc7104c7ceedefa9603015d67477355ef6c36 Mon Sep 17 00:00:00 2001
From: Sean Nyekjaer <sean@geanix.com>
Date: Wed, 24 Mar 2021 21:37:32 -0700
Subject: [PATCH 1/2] squashfs: fix inode lookup sanity checks

commit c1b2028315c6b15e8d6725e0d5884b15887d3daa upstream.

When mouting a squashfs image created without inode compression it fails
with: "unable to read inode lookup table"

It turns out that the BLOCK_OFFSET is missing when checking the
SQUASHFS_METADATA_SIZE agaist the actual size.

Link: https://lkml.kernel.org/r/20210226092903.1473545-1-sean@geanix.com
Fixes: eabac19e40c0 ("squashfs: add more sanity checks in inode lookup")
Signed-off-by: Sean Nyekjaer <sean@geanix.com>
Acked-by: Phillip Lougher <phillip@squashfs.org.uk>
Cc: <stable@vger.kernel.org>
Signed-off-by: Andrew Morton <akpm@linux-foundation.org>
Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
---
 fs/squashfs/export.c              | 10 +++++++---
 fs/squashfs/squashfs_fs_patched.h |  2 ++
 2 files changed, 9 insertions(+), 3 deletions(-)
 create mode 100644 fs/squashfs/squashfs_fs_patched.h

diff --git a/fs/squashfs/export.c b/fs/squashfs/export.c
index eb02072d28dd..ee6df064d41f 100644
--- a/fs/squashfs/export.c
+++ b/fs/squashfs/export.c
@@ -28,7 +28,7 @@
 #include <linux/exportfs.h>
 #include <linux/slab.h>
 
-#include "squashfs_fs.h"
+#include "squashfs_fs_patched.h"
 #include "squashfs_fs_sb.h"
 #include "squashfs_fs_i.h"
 #include "squashfs.h"
@@ -152,14 +152,18 @@ __le64 *squashfs_read_inode_lookup_table(struct super_block *sb,
 		start = le64_to_cpu(table[n]);
 		end = le64_to_cpu(table[n + 1]);
 
-		if (start >= end || (end - start) > SQUASHFS_METADATA_SIZE) {
+		if (start >= end
+		    || (end - start) >
+		    (SQUASHFS_METADATA_SIZE + SQUASHFS_BLOCK_OFFSET)) {
 			kfree(table);
 			return ERR_PTR(-EINVAL);
 		}
 	}
 
 	start = le64_to_cpu(table[indexes - 1]);
-	if (start >= lookup_table_start || (lookup_table_start - start) > SQUASHFS_METADATA_SIZE) {
+	if (start >= lookup_table_start ||
+	    (lookup_table_start - start) >
+	    (SQUASHFS_METADATA_SIZE + SQUASHFS_BLOCK_OFFSET)) {
 		kfree(table);
 		return ERR_PTR(-EINVAL);
 	}
diff --git a/fs/squashfs/squashfs_fs_patched.h b/fs/squashfs/squashfs_fs_patched.h
new file mode 100644
index 000000000000..d993c14e6685
--- /dev/null
+++ b/fs/squashfs/squashfs_fs_patched.h
@@ -0,0 +1,2 @@
+#include "squashfs_fs.h"
+#define SQUASHFS_BLOCK_OFFSET		2
-- 
2.20.1

